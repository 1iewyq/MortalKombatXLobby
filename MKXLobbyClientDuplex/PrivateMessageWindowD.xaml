<Window x:Class="MKXLobbyClientDuplex.PrivateChatWindowD"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Private Chat" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        Background="#FF1A0A0A" FontFamily="Copperplate Gothic Bold">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontFamily" Value="Copperplate Gothic Bold"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Gold"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FFC1272D" Offset="0"/>
                        <GradientStop Color="#FF7B0A0F" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#FFB22222"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True"
                                >
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#FFEF5A5A" Offset="0"/>
                                            <GradientStop Color="#FF9B0F0F" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="BorderBrush" Value="Gold"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF6B0A0A"/>
                                <Setter Property="BorderBrush" Value="#FF4B0202"/>
                                <Setter Property="Foreground" Value="LightGoldenrodYellow"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF555555"/>
                                <Setter Property="Background" Value="#FF2A2A2A"/>
                                <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Background" Value="#FF240909"/>
            <Setter Property="Foreground" Value="Gold"/>
            <Setter Property="BorderBrush" Value="#FFB22222"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="Copperplate Gothic Bold"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="CaretBrush" Value="Gold"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="Gold"/>
                    <Setter Property="Background" Value="#FF3A0D0D"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF555555"/>
                    <Setter Property="Background" Value="#FF1A0A0A"/>
                    <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#FF240909"/>
            <Setter Property="Foreground" Value="Gold"/>
            <Setter Property="BorderBrush" Value="#FFB22222"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontFamily" Value="Copperplate Gothic Bold"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ScrollViewer Focusable="False" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FFB22222"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF7B0A0F"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Gold"/>
            </Style.Resources>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="Gold"/>
            <Setter Property="FontFamily" Value="Copperplate Gothic Bold"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border Background="#FF240909" BorderBrush="#FFB22222" BorderThickness="2" CornerRadius="5"/>
                            <DockPanel>
                                <TextBlock Text="{TemplateBinding Header}" 
                                           Foreground="Gold" FontWeight="Bold" FontSize="14" 
                                           FontFamily="Copperplate Gothic Bold"
                                           Background="#FF7B0A0F" Padding="4" Margin="5" 
                                           DockPanel.Dock="Top" 
                                           HorizontalAlignment="Left"
                                           TextOptions.TextFormattingMode="Display"/>
                                <ContentPresenter Margin="5" />
                            </DockPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="GoldTextBlock">
            <Setter Property="Foreground" Value="Gold"/>
            <Setter Property="FontFamily" Value="Copperplate Gothic Bold"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>
    </Window.Resources>

    <Grid Background="#FF1A0A0A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FF240909" Margin="5" Height="50" VerticalAlignment="Center">
            <TextBlock x:Name="lblPrivateChatWith" Text="Private Chat with [Username]" 
                      Foreground="Gold" FontWeight="Bold" FontSize="20"
                      VerticalAlignment="Center" Margin="15,0"/>
            <Button x:Name="btnClosePrivateChat" Content="Close" 
                    HorizontalAlignment="Right" Margin="0,0,15,0" Width="100" Height="30"
                    Click="BtnClosePrivateChat_Click"/>
        </StackPanel>

        <!-- Chat Messages -->
        <GroupBox Grid.Row="1" Header="Chat Messages" Margin="5">
            <ScrollViewer x:Name="scrollPrivateChat" VerticalScrollBarVisibility="Auto" Background="#FF1A0A0A">
                <TextBlock x:Name="txtPrivateChatMessages" 
                           TextWrapping="Wrap" Foreground="Gold" 
                           Background="Transparent" Margin="5" FontFamily="Copperplate Gothic Bold" FontWeight="Bold"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Message Input -->
        <Grid Grid.Row="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtPrivateMessageInput" Grid.Column="0" 
                     Height="34" FontSize="14" 
                     KeyDown="TxtPrivateMessageInput_KeyDown"/>

            <Button x:Name="btnSendPrivateMessage" Grid.Column="1" Content="Send" 
                    Width="70" Height="34" Margin="5,0,0,0" 
                    Click="BtnSendPrivateMessage_Click"/>
        </Grid>
    </Grid>
</Window>